services:
  - type: web
    name: autobott
    env: python
    plan: starter
    buildCommand: pip install -r autotrader/requirements.txt
    startCommand: python autotrader/render_service.py
    healthCheckPath: /api/status
    envVars:
      - key: ALPACA_API_KEY
        sync: false
      - key: ALPACA_SECRET_KEY
        sync: false

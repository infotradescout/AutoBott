# September 16, 2026 production release

The owner authorized completing production deployment on September 16. PR #42 was merged to main as `65316a0b6bb36cdc80d99f3fc91fb15798ebe5d3`. Its tree exactly matches the previously reviewed candidate tree `1c34649b84545ce4955d649006ab31a474d39501`. Existing Render service `srv-d7bh8lc9c44c73bc1jt0` deployed it as `dep-dalb0fnavr4c739b7cg0`, live at 15:06:47 UTC. The existing persistent disk and broker credentials were preserved. The operator credential was configured privately, not committed. Paper mode was explicitly pinned; live-money trading remained disabled.

## Runtime verification

Authenticated GET-only acceptance at 15:19:00 UTC and 15:32:18 UTC verified the exact deployed revision and `hosted-core-runner-v2` policy, new cockpit HTML, generic 401 responses on protected routes, successful authenticated account/position/pair/session/timeline reads, and paper-only controls. The session advanced from 6 to 13 completed cycles. No broker POST, cancellation, position liquidation, or journal correction was performed by the acceptance script. Normal deployed bot monitoring was not disabled.

This is not complete operational reconciliation. The production cycle explicitly rejected new entries with `outcome_journal_reconciliation_required`; the existing account-derived journal must be reconciled without deleting history or bypassing the gate. The read-only timeline deliberately does not supply a verified account scope and therefore its journal diagnostic is not a substitute for the runtime ingestion diagnostic. Broker-versus-stored option quantity mismatch also remains; do not arbitrarily assign an exit to an entry lot or assert clean inventory from symbol-only matching. Account-specific evidence is retained in private owner logs, not this public document.

## Follow-up correction

The V2 adapter temporarily replaced shared legacy module builder and monitor functions. That created a concurrency hazard. The correction binds those two dependencies in a private function namespace for each invocation, preserving the shell code, broker gates, journal helpers, shared lock identities, defaults and closure. It never replaces a module attribute. Five regressions cover overlapping calls, nested calls, exception handling, keyword defaults and unsupported shell fail-closed behavior. Temporary diagnostic instrumentation was removed.

On exact commit `80fb2dd57af2f35786093b60d8ff061916519577`, Render ran **563 Python tests plus 16 subtests (579 JUnit cases)** successfully on Python 3.12.8 and pytest 9.1.1, with no failures/skips and zero denied external-operation attempts. All **11 cockpit Node tests** passed. Source identity and manifest were unchanged; manifest `48ee990dde89dabf75c8166d0ca3796edb23fb90df77c65cb2d986fa44dc5b70`. Nine GET-only acceptance tests ran in the independent acceptance build before its successful runtime check. Earlier failed runs remain evidence and were not represented as passing.

The acceptance helper prioritizes `option_symbol` for stored records that also include an underlying `symbol`, and keeps quantity mismatches and incomplete accounting explicit. It never submits orders and never publishes account data as static assets.

## Next required accounting work

Preserve an immutable snapshot, obtain the detailed account-scoped runtime reconciliation plan, construct a separately broker-reconciled view and explicit correction map, and resolve remaining-lot ownership before allowing the entry gate to clear. Do not reset a journal, relabel uncertain ownership, lower an entry guard, or treat `releaseVerified` as evidence of strategy profitability.
